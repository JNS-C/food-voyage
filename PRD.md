# food-voyage — PRD

> 제품 요구사항 정의서
> 작성일: 2026-08-14 (DAY 4) · 최종 수정: 2026-08-14 (v1.4)
> 최종 발표: 2026-08-28
> 디자인 값의 단일 출처는 `DESIGN.md`다. 이 문서는 값을 복사해 적지 않는다.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 이름 | food-voyage |
| 한 줄 설명 | 한 곳씩 개척하고 기록하는 미식 항해일지 |
| 유형 | 개인 기록형 + 읽기 공개 웹 서비스 |
| 난이도 | 중 |
| **오늘(8/14) 범위** | **랜딩페이지 1장. 그 외 화면 없음** |

### 서비스 한 문장 정의

> **food-voyage**는 늘 가던 가게만 가는 게 답답한 사람이, 밥을 먹고 난 직후에, 다녀온 곳을 항해일지처럼 남겨 내 동네 지도를 채우고 아직 비어 있는 구역을 발견하기 위해 쓰는 맛집 기록 서비스다.

### 이름의 의미

`voyage`는 `trip`(짧은 다녀옴)이나 `tour`(정해진 코스)가 아니라 **긴 항해와 탐험**을 뜻한다. 이 서비스는 맛집을 빨리 찾아주는 도구가 아니라, **내 미식 여정이 축적되는 기록물**이다. 이름에서 파생된 은유가 서비스 전체를 관통한다.

| 은유 | 서비스 요소 |
|---|---|
| 항해일지 (logbook) | 리뷰 = 기록물, 날짜순 아카이브 |
| 미개척 해역 | 아직 안 가본 동네 = 여백, 채우고 싶은 동기 |
| 개척 | 새로운 가게 첫 방문 |

---

## 2. 사용자 시나리오

### 누가 · 언제 · 왜

- **누가** — 생활 반경이 좁아져 늘 가던 3~4곳만 반복하는 동네 생활자
- **언제** — 식사 직후(기록), 다음 끼니를 정할 때(탐색)
- **왜** — 다녀온 곳을 쌓아 내 동네 지도를 채우고, 아직 안 가본 곳을 발견하려고

### 경쟁 구도

네이버 지도·망고플레이트와 겨루지 않는다.

| | 기존 서비스 | food-voyage |
|---|---|---|
| 파는 가치 | 모르는 동네에서 **실패하지 않기** | 아는 동네를 **다 밟아보기** |
| 사용 시점 | 가기 **전** | 갔다 **온 후** |
| 순위 기준 | 별점 평균 | **재방문율** |

이 구분은 이후 모든 카피와 기능 판단의 기준이 된다.

### 대표 시나리오

**① 기록 흐름**
점심에 처음 보는 국숫집에 들어간다 → 나오면서 앱을 연다 → 한 줄을 남기고 **재방문 판정**에 답한다 → 내 동네 격자에 한 칸이 채워진다.

**② 탐색 흐름**
저녁에 뭘 먹을지 모르겠다 → 랜딩에서 동네 이름을 검색한다 → **10명 중 8명이 또 간 집**이 보인다 → 처음 가보는 곳을 고른다.

**③ 방문자 흐름 (로그인 전)**
공유받은 링크로 들어온다 → 남들이 쌓아둔 항해일지가 흐르는 걸 본다 → "나도 내 동네를 이렇게 채울 수 있겠다"고 느낀다.

---

## 3. 핵심 기능 목록

### 3-1. 오늘(8/14) 만드는 것 — 이것만

**랜딩페이지 한 장.** 리뷰 데이터·로그인·상세 페이지는 오늘 만들지 않는다. 아래 기능 목록은 랜딩에서 **소개되는 대상**일 뿐 구현 대상이 아니다.

### 3-2. 필수 기능 (서비스 전체)

| 기능 | food-voyage에서의 의미 | 구현일 |
|---|---|---|
| 리뷰 기록 | 항해일지 한 편 작성 | 8/18 |
| 리뷰 리스트 + 검색·필터 | 지나온 항로 되짚기 (JSON) | 8/18 |
| **재방문 판정** | "또 갈지"를 한 번에 답하는 필드 | 8/18 |
| 스크롤·호버 애니메이션 | 섹션 진입 시 페이드업 (양방향) | **8/14 완료** |
| 모바일 최적화 | 반응형 | 8/19~20 |
| AI 리뷰 요약·감성 분석 | 일지를 한 줄로 압축 (Gemini) | 8/21 |
| 로그인·데이터 저장 | 내 항해일지가 남음 (Supabase) | **8/24 완료** |
| 도넛 차트·워드클라우드 | 항해 기록 요약 대시보드 | 8/25 |

**재방문 판정이 이 서비스의 축이다.** 별점 평균은 어디에나 있지만 재방문율 랭킹은 흔치 않다. 이 필드 하나가 인기 순위(8/18)와 도넛 차트(8/25)의 데이터 원천이 된다.

**데이터 모델 — 3상태 고정.** 불린으로 만들지 않는다.

```
willRevisit: 'yes' | 'once' | 'maybe'
```

| 값 | 뜻 | 쓰이는 곳 |
|---|---|---|
| `yes` | 또 갈 곳 | 재방문율 계산, 랜딩 노출 |
| `once` | 한 번이면 충분 | 도넛 차트 |
| `maybe` | 애매함 | 도넛 차트 |

- 재방문율 = `yes` 비율. 인기 순위(8/18)와 카드 표기의 근거값이다.
- 도넛 차트(8/25) = 세 값의 비율. 8/18에 불린으로 만들면 그때 데이터를 갈아엎어야 한다.
- 워드클라우드 = 리뷰에서 자주 쓴 표현

**별점은 만들지 않는다.** §2 경쟁 구도가 순위 기준을 별점 평균에서 재방문율로 바꾸는 것인데, 별점을 입력받아 두면 결국 어딘가에 표시하게 되고 차별점이 희석된다. 평가는 재방문 3상태가 대신한다.

### 3-3. 로드맵 (오늘 범위 밖)

| 시점 | 내용 |
|---|---|
| 8/26~27 | 배포 · SEO · OG 이미지 |
| 8/28 | 최종 발표 |
| 이후 | 실제 지도 API 연동, 사진 업로드, 친구 항해일지 팔로우 |

**지도 개척 기능은 로드맵으로 분리한다.** 커리큘럼에서 지도 API를 다루지 않아 일정 리스크가 크다. 대신 "빈 구역이 보인다"는 감각만 **동네 격자 UI**(다녀온 곳은 채워지고 안 간 곳은 비어 있음)로 구현하고, 8/25 대시보드 작업에 흡수한다.

---

## 4. 기술 스택

| 구분 | 8/14 ~ 8/21 | 8/24 이후 (확정) |
|---|---|---|
| 마크업 | HTML | HTML |
| 스타일 | Tailwind CDN | Tailwind CDN |
| 스크립트 | Vanilla JS | Vanilla JS |
| AI | — | Google Gemini API (Vercel 함수) |
| DB·인증 | — | Supabase (supabase-js UMD, CDN) |
| 차트 | — | Chart.js |
| 배포 | GitHub + Vercel 자동 배포 | 동일 |

**§8의 컷오프 규칙이 발동했다.** 8/23 밤까지 Next.js 이전이 끝나지 않았으므로 바닐라로 확정한다. 아래 전환 계획은 그 판단의 근거로 남겨 둔다.

8/24 로그인이 그 확정을 검증했다 — 인증·세션·프로필을 프레임워크 없이 붙였고, 막힌 곳은 없었다. `js/auth.js` 하나가 `window.FvAuth`를 내보내고 세 페이지가 그걸 공유한다.

### 스택 전환 계획 (8/23에 종료)

**8/21 Gemini API 수업 직후 재판단하고, 이전한다면 8/22~23 주말에 수행한다.**

- **이전이 필요한 이유** — 8/24 Supabase 인증 세션 처리, 8/25 대시보드의 상태 관리, 8/26~27 SEO·OG 이미지에서 Next.js가 유리하다.
- **Tailwind로 짜두는 이유** — 클래스가 JSX로 그대로 옮겨진다. `class` → `className` 치환이 이전 비용의 전부다.
- **주말에 하는 이유** — 수업 진도를 하나도 뺏기지 않는다.

### API 키 처리 (스택과 별개)

**Gemini API 키는 어떤 스택에서도 클라이언트 코드에 두지 않는다.** 브라우저가 받아가는 파일에 넣으면 배포된 순간 누구나 열어볼 수 있다. `.gitignore`로 그 파일을 제외해도 보호되지 않으며, 오히려 배포에서 빠져 기능이 깨진다.

**바닐라 스택에서도 서버를 쓸 수 있다.** 프로젝트 루트에 `api/gemini.js`를 두면 Vercel이 서버리스 함수로 배포한다. 키는 Vercel 대시보드의 환경변수에 넣고, 브라우저는 `/api/gemini`만 호출한다. 프레임워크가 필요 없다.

따라서 **키 문제는 스택 전환의 근거가 아니다.** 8/21에는 바닐라 + Vercel 함수를 먼저 시도하고, 막히면 그때 이전 쪽으로 판단한다.

### 8/25: 이 구조를 처음 실행에 옮겼다 — `api/place-photo.js`

위 문단은 계획이었고, 첫 서버 함수는 Gemini가 아니라 **구글 Places 사진**이 됐다. 판단은 그대로 유효했다 — 프레임워크 없이 `api/*.js` 하나로 끝났다.

| 항목 | 값 |
|---|---|
| 환경변수 | `GOOGLE_PLACES_KEY` (Vercel · Production/Preview/Development) |
| 키 제한 | **API 제한만** — Places API (New) 전용. HTTP referrer 제한은 걸지 않는다(서버 키이고 Vercel 이그레스 IP는 고정이 아니다) |
| FieldMask | `places.id,places.photos,places.location,places.displayName` — **여기서 늘리지 않는다.** FieldMask가 과금 SKU 등급을 정하고, `rating`·`reviews`는 상위 등급으로 올린다 |
| 키 노출 방지 | `…/media?skipHttpRedirect=true`가 주는 **서명된 URL만** 브라우저에 넘긴다. 그 URL에는 키가 없다 |
| 캐시 | L0 인메모리(페이지) + L1 `localStorage` 30일. **`photoName`만 저장하고 단명하는 `photoUri`는 저장하지 않는다** |
| 호출 상한 | 카드 앞 8행 (`PHOTO_LIMIT`) |
| 로컬 실행 | `npx vercel dev --listen 8000` — 포트 8000이어야 카카오 키 허용목록에 걸린다 |

**§6-2의 "4:3 사진(8/18~)"이 여기서 실현됐다.** 다만 검색 결과는 리스트형 행이라 72px 정사각형이고, 4:3은 랜딩 카드(`{component.card-place}`)에 남아 있는 약속이다.

**엉뚱한 가게에 사진을 붙이지 않는다.** §8「하지 말 것」5의 "실제 가게 이름에 가짜 평가를 붙이지 않는다"와 같은 계열의 규율이다. 거리(200m)와 이름(포함 또는 bigram Dice ≥ 0.5) 두 관문을 AND로 통과해야 하고, 떨어지면 색면을 남긴다.

> **미해결 준수 부채 — 사진 저작자 표시.**
> 구글 약관은 사진과 함께 `authorAttributions`를 **표시하도록 요구한다.** 72px 행 썸네일에는 표기 공간이 없어 지금은 썸네일 컨테이너의 `title` 속성에 싣는 최소 준수 상태다. 화면에 보이지 않으므로 **완전한 준수가 아니다.** 상세 화면이 생길 때 함께 해결한다. 발표 범위에서는 이대로 가되, 모르고 넘어가는 것과 알고 넘어가는 것은 다르므로 여기 적어 둔다.

---

## 5. 화면 구성

### 5-1. 전체 화면 (최종 목표)

`/` 랜딩 · `/list` 리뷰 목록 · `/review/[id]` 상세 · `/write` 작성 · `/dashboard` 대시보드 · `/login`

**오늘은 `/` 하나만 만든다.**

### 5-2. 랜딩페이지 섹션 — 스토리 스크롤형

서사 구조: **비어 있다 → 채운다 → 쌓인다 → 더 온다**

| 순서 | 섹션 | 서사 역할 | 내용 |
|---|---|---|---|
| 1 | 히어로 | 비어 있다 | 서비스명 + 한 줄 소개 + 동네 검색창 + 시작 버튼 |
| 2 | 인기 맛집 카드 | 채운다 | 재방문율 순 카드 6개 |
| 3 | 최근 항해일지 | 쌓인다 | 한 줄 인용 + 재방문 판정이 가로로 흐름 |
| 4 | 서비스 특징 | 더 온다 | AI 요약·감성 분석·대시보드, Coming Soon 뱃지 |
| 5 | 푸터 | — | 만든 사람, GitHub, 프로젝트 허브 링크 |

섹션 사이에 구분선을 넣지 않는다. **배경색이 밝음↔어두움으로 교차하는 것 자체가 구분선이다.**

### 5-3. 두 요소의 정체 (중요)

| 요소 | 일반적 형태 | food-voyage |
|---|---|---|
| 검색창 | 가게명부터 친다 | **동네를 먼저 고른다** (지역 칸 + 키워드 칸) |
| 인기 순위 | 별점 평균 순 | **재방문율 순** (`10명 중 8명이 또 갔어요`) |

가게명부터 치는 것은 기존 맛집 앱의 문법이다. 동네가 먼저 오는 순서가 개척 컨셉을 강화한다.

**8/21 수정.** 지역은 고정 목록이 아니라 자유 입력이고, 카카오가 해석하는 곳이면 어디든 값이 된다. 지역을 좁힌 다음이라면 키워드로 가게명을 쳐도 된다 — 정체성을 지키는 것은 "무엇을 칠 수 있는가"가 아니라 **지역이 먼저라는 순서**다. 자세한 내용은 `DESIGN.md`의 `search-neighborhood`·`region-suggest` 항목.

---

## 6. 상세 기능 명세

### 6-1. 오늘의 인터랙션 범위 — "가짜 반응"

죽은 버튼은 고장난 사이트처럼 보인다. 누르면 **안내 토스트**가 뜨게 해서 "만들어지는 중인 서비스"라는 신호를 준다.

| 요소 | 오늘(8/14) 동작 | 이후 |
|---|---|---|
| 동네 검색창 | 입력 가능, 엔터 시 토스트 `동네 검색은 8월 18일에 열립니다` | 실제 검색 (8/18) |
| 시작 버튼 | 토스트 `로그인은 8월 24일에 열립니다` | 로그인 (8/24) |
| 맛집 카드 | 호버 시 반응, 클릭 시 토스트 `리뷰 상세는 8월 18일에 열립니다` | 상세 페이지 (8/18) |
| 최근 항해일지 | 가로 스크롤만 동작 | 실데이터 (8/18) |
| 특징 카드 | Coming Soon 뱃지 표시, 클릭 반응 없음 | 실기능 연결 |
| 푸터 링크 | **실제로 작동** (GitHub·프로젝트 허브) | 유지 |

토스트는 3초 후 자동으로 사라진다. 화면 하단 중앙에 뜬다.

### 6-2. 맛집 카드 구성 요소

① 색면 썸네일 (오늘) → 4:3 사진 (8/18~) · ② 가게명 · ③ 동네 칩 · ④ **재방문율** · ⑤ 호버 반응

**여기서 더 얹지 않는다.** 별점 평균·리뷰 수·가격대·영업시간·한 줄 요약을 추가하는 순간 흔한 맛집 카드가 된다. 카드에 등장하는 숫자가 재방문 비율 하나뿐이어야 그것이 차별점으로 읽힌다.

재방문율의 표기 형태(닻 5개 + 비율 숫자)와 카드 치수는 `DESIGN.md`의 `card-place`, `anchor-rating` 항목을 따른다.

### 6-3. 가짜 데이터 명세

카드 6개, 항해일지 5개를 배치한다. 상호는 **가상**으로 짓는다. 실제 가게 이름에 없는 평가를 붙이지 않기 위해서다. 일반 명사 조합은 실존 가능성이 높으니 살짝 특이하게 짓는다.

변수·파일명에 `mock` 접두사를 붙인다 (`mockRestaurants`, `mock-data.js`). 8/18에 갈아끼울 자리가 눈에 보여야 한다.

**사용자는 두 명이다.** 서로 다른 동네에 사는 별개의 사용자로 설정한다.

| 사용자 | 생활권 | 역할 |
|---|---|---|
| A | 고양시 일산 일대 | 항해일지 3편 |
| B | 서울 성수 일대 | 항해일지 2편 |

두 명을 두는 이유가 두 가지다. 첫째, 재방문율이 "10명 중 8명"이라는 **집계값**이려면 애초에 사용자가 여럿이어야 한다. 한 사람의 데이터로는 그 숫자가 성립하지 않는다. 둘째, 이 서비스는 **읽기 공개**(§2 시나리오 ③)라서 랜딩 3번 섹션에 흐르는 것은 남의 일지다. 작성자가 둘이어야 그 구조가 화면에서 읽힌다.

따라서 항해일지 카드에는 **작성자를 표기한다.** 표기 형태는 `DESIGN.md`의 `quote-log`를 따른다.

### 동네 목록

| 생활권 | 동네 |
|---|---|
| 일산 (고양시) | 정발산 · 마두 · 백석 · 주엽 · 대화 · 탄현 · 풍동 |
| 성수 (서울) | 성수 · 서울숲 · 뚝섬 · 연무장길 · 왕십리 · 건대입구 |

**표기는 역·지역명으로 통일한다.** `정발산동`과 `정발산역`을 섞지 않는다. 동네 칩에 들어가는 문자열이 짧고 일정해야 카드 그리드가 조용해진다.

카드 6개는 두 생활권에 3개씩 배분한다.

### 데이터 구조

```
mockUsers        { id, name, homeArea }
mockRestaurants  { id, name, neighborhood, area, revisitYes, revisitTotal, initial, tone }
mockLogs         { id, userId, quote, restaurantName, neighborhood, date, willRevisit }
mockFeatures     { title, description, releaseDate }
```

- `revisitYes / revisitTotal`로 두고 비율은 계산해서 쓴다. 반올림된 값을 저장하면 8/25 차트에서 원본이 필요해진다.
- `willRevisit`은 `'yes' | 'once' | 'maybe'` 3상태다 (§3-2). 랜딩에는 `yes`인 일지만 노출한다.
- `mockRestaurants`는 재방문율 내림차순으로 정렬해 둔다.

---

## 7. 디자인 가이드

> **디자인 값의 단일 출처는 `DESIGN.md`다.** 색상 hex, 타이포 스케일, 여백·라운드 수치, 컴포넌트 명세는 전부 그쪽에 있다. 이 문서는 값을 복사하지 않고 **결정된 방향만** 적는다.

### 컨셉 한 줄

**항해일지를 애플의 진열 방식으로 전시한다.**

색과 은유는 항해일지에서, 구조와 절제는 애플 디자인 시스템에서 가져온다. 두 레퍼런스가 서로 다른 축을 담당하므로 충돌하지 않는다.

### 확정된 방향

- **온도** — 기록실 + 온기 한 스푼. 크림이 기본 캔버스이고, 딥 네이비는 히어로와 항해일지 섹션 두 곳에만 쓴다.
- **액센트** — 황동 한 색. 두 번째 강조색은 존재하지 않는다.
- **서체** — Pretendard 하나. 위계는 서체를 바꿔서가 아니라 **크기와 굵기(400/600)로만** 만든다. 강조색이 황동 하나인 것과 같은 이유다. 워드마크만 세리프를 쓰는 예외이며, 그 예외를 늘리지 않는다.
- **섹션 구분** — 선이 아니라 배경색 교차.
- **그림자** — 음식 사진에만. 색면 썸네일에는 주지 않는다.
- **재방문율 표기** — 별이 아니라 **닻**. 집계값(인기 맛집)은 닻 5개 + 비율 숫자, 개인 기록 한 편(항해일지)은 닻 1개 + 라벨.
- **은유 분담** — 깃발은 개척(첫 방문), 닻은 재방문. 로고에는 둘 다 쓰지 않는다.

구체적인 값이 필요할 때는 `DESIGN.md`의 해당 컴포넌트 키를 참조한다 (`card-place`, `anchor-rating`, `quote-log` 등).

### 로고 · 앱 아이콘

생성 프롬프트와 검증 절차는 `LOGO_PROMPT.md`에 있다. 워드마크는 소문자 세리프 `food-voyage`다. 본문 서체가 Pretendard 하나로 통일된 뒤에도 **로고만 세리프를 유지한다** — 로고는 읽는 글이 아니라 표식이고, 서체가 다른 것이 오히려 로고임을 알린다. 값은 `DESIGN.md`의 `{typography.wordmark}`.

---

## 8. 제약사항

### 하지 말 것

1. **오늘은 랜딩 1장만.** `/login`, `/detail` 등 다른 페이지를 만들지 않는다.
2. **두 번째 강조색을 만들지 않는다.** 모든 "누를 수 있음" 신호는 `accent` 황동 하나다.
3. **그림자는 사진에만.** 카드·버튼·텍스트에 그림자를 넣지 않는다.
4. **디자인 값을 중복 기재하지 않는다.** 컬러·폰트의 출처는 `DESIGN.md` 하나다.
5. **실제 가게 이름에 가짜 평가를 붙이지 않는다.** 상호는 가상으로 짓는다.
6. **배경 그라디언트를 장식으로 쓰지 않는다.**
7. **별점을 만들지 않는다.** 평가는 재방문 3상태가 대신한다.
8. **API 키를 클라이언트 코드에 두지 않는다.** `.gitignore`는 이 문제를 해결하지 못한다. 서버(Vercel 함수 또는 Next.js 라우트)를 반드시 거친다.

### 지킬 것

1. **가짜 데이터에는 `mock` 접두사.**
2. **커밋·푸시 습관.** 작업 단위마다 커밋한다. 문서는 `docs:`, 기능은 `feat:` 접두사를 쓴다.
3. **모바일 확인.** 모든 작업 후 좁은 화면에서 한 번 본다.
4. **재방문 판정은 3상태로 유지한다.** 편의상 불린으로 줄이지 않는다.
5. **외부 CDN은 버전을 고정한다.** 태그 없는 경로는 업스트림 변경 시 발표 당일에 깨질 수 있다.

### 컷오프 규칙

**8/23(일) 밤까지 Next.js 이전이 끝나지 않으면 바닐라 스택으로 확정하고 더 건드리지 않는다.** 발표는 8/28이고, 스택 전환이 발표 준비를 잡아먹으면 안 된다.

바닐라로 확정되어도 **기능을 포기하지 않는다.** AI 요약은 Vercel 서버리스 함수로, 인증과 데이터는 Supabase 클라이언트 라이브러리로 처리할 수 있다. 이전은 편의의 문제이지 가능·불가능의 문제가 아니다.

---

## 오늘의 완료 기준

- [ ] 5개 섹션이 모두 있는 랜딩페이지
- [ ] 데스크탑·모바일 양쪽에서 깨지지 않음
- [ ] 버튼·검색창 클릭 시 토스트 동작
- [ ] Vercel 배포 URL 생성
- [ ] 프로젝트 허브 카드가 Live로 전환
- [ ] `PRD.md` · `DESIGN.md` 커밋

---

## 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v1.7 | 2026-08-25 | **검색 결과 카드 썸네일에 구글 Places 사진 1장을 붙였다.** §4에 `api/place-photo.js` 결정 기록을 추가 — 이 저장소의 첫 서버리스 함수이고, v1.1이 세운 "바닐라에서도 Vercel 함수로 키를 숨긴다"를 처음 실행에 옮긴 것이다. 색면을 **교체가 아니라 덮는** 구조라 로딩·실패·미매칭이 전부 자동으로 색면으로 떨어진다(상태 블록을 안 늘렸다). 오매칭 방어는 거리 200m + 이름 유사도 두 관문 AND. 저작자 표시가 미해결 준수 부채로 남아 §4에 기록했다. |
| v1.6 | 2026-08-24 | **이메일 인증을 켠다.** v1.5의 계획은 Confirm email을 끄는 것이었으나, 그 판단의 근거는 "인증이 불필요하다"가 아니라 "기본 메일러가 시간당 2통이라 배달을 못 믿는다"였다 — 기능의 문제가 아니라 발송량의 문제이므로 인증을 빼는 근거가 되지 않는다. 클라이언트가 켜짐·꺼짐을 둘 다 다루게 고쳐서 대시보드 토글 하나로 갈리게 했다. 기본 SMTP로 시작하되 배달 여부를 먼저 확인하고, 안 되면 Resend·Brevo로 갈아탄다. 발표용 계정 A·B는 Gmail + 주소로 만든다 — **시간당 2통이 계정 2개로 소진되므로 발표 전날까지 만들어 둔다.** |
| v1.5 | 2026-08-24 | §3-2 로그인·데이터 저장을 **8/24 완료**로. §4 스택 표를 **바닐라 확정**으로 갱신 — 8/23 밤 컷오프가 발동했고, 로그인을 프레임워크 없이 붙여 그 판단을 검증했다. 인증 방식은 **이메일+비밀번호**로 결정했다(구글 OAuth 아님): `profiles`에 생활권을 받는 이상 온보딩 단계가 어느 쪽이든 필요해 구글의 이점이 상쇄되고, §6-3이 요구하는 사용자 두 명을 발표에서 갈아타려면 이메일 계정 2개가 구글 계정 2개보다 싸다. Supabase는 두 방식을 같은 `auth.users`로 다루므로 구글은 나중에 덧붙이면 된다. `/login` 페이지와 `supabase/schema.sql`(profiles + RLS) 추가. |
| v1.0 | 2026-08-14 | 최초 작성 (DAY 4) |
| v1.1 | 2026-08-14 | §4 스택 전환 근거에서 API 키 항목 삭제, API 키 처리 절을 신설. §6-2 카드 구성에서 "한 줄 요약" 제거 (DESIGN.md `card-place`와 충돌). §7 팔레트·서체·레이아웃 값 표 삭제 후 방향 요약으로 대체 (§8-4 단일 출처 규칙 위반 해소). §3-2에 재방문 3상태 데이터 모델 명시. §2·§5-2에서 별점 제거. 제약사항 2건 추가. |
| v1.4 | 2026-08-14 | §7 워드마크를 세리프로 되돌림 — 본문은 Pretendard 하나를 유지하되 로고만 예외로 둔다. 상세는 `DESIGN.md` alpha.4. |
| v1.3 | 2026-08-14 | §7 서체 방향을 **Pretendard 단일 서체**로 변경 (명조 제거). §7 워드마크도 Pretendard로. §3-2 스크롤 애니메이션을 8/19~20에서 **8/14 완료**로 앞당김. 상세는 `DESIGN.md` alpha.3. |
| v1.2 | 2026-08-14 | mock 데이터 무대를 일산·성수 두 생활권으로 변경하고, 서로 다른 사용자 2명 구조를 §6-3에 명시. 동네 목록·데이터 구조 추가. 검색 placeholder를 `정발산역`으로 변경. |
