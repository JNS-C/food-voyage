# CLAUDE.md

`food-voyage` — 내 동네를 한 곳씩 개척하고 기록하는 미식 항해일지.

## 단일 출처

| 무엇 | 어디 |
|---|---|
| 디자인 값 (색·타이포·여백·컴포넌트 명세) | `DESIGN.md` |
| 범위·기능·데이터 모델·일정 | `PRD.md` |

**이 파일은 값을 복사하지 않는다.** hex나 px가 필요하면 `DESIGN.md`의 컴포넌트 키(`card-place`, `anchor-rating`, `quote-log`, `card-feature` 등)를 찾아본다. 같은 값을 세 곳에 두면 하나가 바뀔 때 조용히 어긋난다 (PRD §8-4).

## 현재 상태

문서만 있고 코드는 없다. 오늘(8/14) 범위는 **랜딩페이지 한 장**이다. `/list` `/write` `/login` 등 다른 페이지를 만들지 않는다.

## 실행

빌드 단계가 없다. 설치할 것도 없다.

```
index.html을 브라우저로 열거나
npx serve
```

Tailwind는 CDN, 스크립트는 바닐라 JS다. 콘솔에 뜨는 Tailwind Play CDN 프로덕션 경고는 정상이다.

## 파일

| 파일 | 책임 |
|---|---|
| `index.html` | 마크업 · Tailwind config(디자인 토큰 등록) · 폰트 로딩 · SVG symbol |
| `mock-data.js` | 가짜 데이터. 8/18에 통째로 갈아끼울 자리 |
| `script.js` | 렌더링 · 토스트 |

## 깨지기 쉬운 규칙

두 문서 안쪽에 묻혀 있어 놓치기 쉬운 것들이다. 어기면 서비스의 차별점이나 디자인 일관성이 무너진다.

### 정체성

- **별점을 만들지 않는다.** 평가는 재방문 3상태(`yes` / `once` / `maybe`)뿐이다. 별점을 입력받아 두면 결국 어딘가에 표시하게 되고, 재방문율이라는 차별점이 희석된다
- 재방문 판정을 불린으로 줄이지 않는다. 8/25 도넛 차트가 세 값을 모두 쓴다
- 재방문율은 반올림값을 저장하지 않고 `yes / total`로 계산한다
- 검색은 **동네** 검색이다. 가게명 검색은 기존 맛집 앱의 문법이라 정체성이 흐려진다
- 가짜 데이터에는 `mock` 접두사. 상호는 가상으로 짓고, 실제 가게 이름에 없는 평가를 붙이지 않는다

### 닻 표기

- 집계값(인기 맛집)은 **닻 5개 + 비율 숫자**, 개인 기록 한 편(항해일지)은 **닻 1개 + 라벨**. 섞지 않는다 — 같은 기호가 두 뜻이 되면 서명이 흐려진다
- `once` · `maybe`에는 닻을 아예 표시하지 않는다. 빈 닻을 두면 "평가가 나쁘다"로 읽혀 3상태의 뉘앙스가 무너진다
- 재방문율에 별이나 깃발을 쓰지 않는다. 깃발은 개척(첫 방문) 담당이다

### 시각

- 두 번째 강조색을 만들지 않는다. "누를 수 있음" 신호는 황동 하나다. 누를 수 없는 것(Coming Soon 뱃지 등)에는 쓰지 않는다
- 그림자는 음식 사진에만. **색면 썸네일에는 주지 않는다** — 색면은 놓인 물체가 아니라 빈 자리 표시다. 사진이 들어오는 8/18에 함께 추가한다
- 화이트 타일 위에 화이트 카드를 놓지 않는다
- 섹션 구분에 선을 긋지 않는다. 배경색 교차가 구분선이다
- 마크업에 인라인 hex를 쓰지 않는다. Tailwind config에 등록한 토큰만 쓴다
- 굵기는 400과 600만. 500은 없다
- 한글에 `font-style: italic`을 걸지 않는다. 브라우저가 만드는 합성 오블리크가 명조를 뭉갠다

### 안전

- **API 키를 클라이언트 코드에 두지 않는다.** `.gitignore`는 이 문제를 해결하지 못한다 — 막으면 배포에서 빠져 기능이 죽고, 배포하면 브라우저에서 그대로 보인다. 키는 Vercel 환경변수에 두고 서버(`api/*.js` 또는 Next.js 라우트)를 거친다. `api/` 자체는 커밋 대상이다
- 외부 CDN은 버전 태그를 고정한다. 태그 없는 경로는 업스트림이 바뀌면 발표 당일에 깨진다

## 커밋

작업 단위마다 커밋한다.

| 접두사 | 용도 |
|---|---|
| `feat:` | 기능 |
| `docs:` | 문서 |
| `chore:` | 설정 |

## 일정

"오늘 이걸 해야 하나"의 판단 기준이다.

| 날짜 | 내용 |
|---|---|
| 8/14 | 랜딩페이지 (오늘) |
| 8/18 | 리뷰 기록·목록·상세, 실데이터 교체 |
| 8/19~20 | 스크롤 애니메이션, 모바일 최적화 |
| 8/21 | Gemini AI 요약·감성 분석 |
| 8/24 | Supabase 로그인·저장 |
| 8/25 | 도넛 차트·워드클라우드 |
| 8/26~27 | 배포·SEO·OG 이미지 |
| 8/28 | 최종 발표 |

**컷오프** — 8/23 밤까지 Next.js 이전이 끝나지 않으면 바닐라로 확정하고 더 건드리지 않는다. 바닐라로 가도 기능을 포기하지 않는다. AI는 Vercel 서버리스 함수로, 인증·데이터는 Supabase 클라이언트 라이브러리로 된다. 이전은 편의의 문제이지 가능·불가능의 문제가 아니다.
