/**
 * 이 파일을 복사해 `config.js`로 저장하고 키를 채운다. config.js는 커밋되지 않는다.
 *
 * 카카오 **JavaScript 키**다. REST 키가 아니다.
 * JavaScript 키는 브라우저 노출이 전제된 키이고, Kakao Developers의
 * 플랫폼 > Web 사이트 도메인 등록으로 방어된다. 로컬에서 쓰려면
 * `http://localhost:8000`을 등록해 둬야 한다.
 */
window.KAKAO_JS_KEY = '';
